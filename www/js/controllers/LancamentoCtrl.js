app.controller("LancamentoCtrl", function (
    $rootScope,
    banco,
    $interval,
    $timeout,
    $ionicHistory,
    $ionicLoading,
    $ionicModal,
    $sce,
    $scope,
    $stateParams,
    $state,
    ionicMaterialInk,
    DatabaseValues,
    $filter,
    $ionicPopup,
    $cordovaGeolocation,
    $timeout,
    $ionicScrollDelegate,
    $ionicPlatform,
    $timeout,
    $cordovaBarcodeScanner,
    $location
  ) {
    try {

    } catch (ex) {
      alert(ex.message);
    }
  });
  